//
//  NavigationBar.swift
//  alarm2
//
//  Created by olderor on 17.10.16.
//  Copyright © 2016 olderor. All rights reserved.
//

import Foundation
import UIKit

class MainView: UIView {
    
    @IBAction func onAddBarButoonItemClick(_ sender: UIBarButtonItem) {
        /*
        let storyboard = UIStoryboard(name: "Main", bundle: nil)
        let popoverController = storyboard.instantiateViewController(withIdentifier: "datePicker") as! EditAlarmViewController
        popoverController.modalPresentationStyle = UIModalPresentationStyle.popover
        let vc = self.parentViewController
        if vc == nil {
            print("Failed to get parentViewController")
            return
        }
        vc!.present(popoverController, animated: true, completion: nil)*/
    }
}
